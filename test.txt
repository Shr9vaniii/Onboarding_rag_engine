"""Summarise the given JSON and
Reply with the following JSON structure:
{
id: The issue ID specified in the provided content.
Issue: The summarised issue in the provided content.
Solution: The propsed solution in the content that solves the issue.
url: The url specified in the provided content.
}
Provided Content:
{
    "id": 136,
    "title": "using alias in response schema",
    "url": "https://github.com/fastapi/fastapi/issues/136",
    "body": "**Describe the bug**\r\nWhen using alias in response schema, the documentation uses the alias in the response but the returned response does not use the alias.\r\n\r\n\r\n**To Reproduce**\r\nSteps to reproduce the behavior:\r\n\r\n```python\r\nclass Test(BaseModel):\r\n    test: int = Schema(..., alias=test2)\r\n\r\n@router.get(\"/test\", response_model=Test)\r\ndef test():\r\n    return {\"test\": 1}\r\n\r\n# in test.py\r\n>> response = client.get(\"/test\", …)\r\n>> response.json()\r\n{\"test2\": 1}\r\n```\r\n\r\n**Expected behavior**\r\nI would expect the doc and the code to be consistent.\r\n\r\n**Screenshots**\r\nIf applicable, add screenshots to help explain your problem.\r\n\r\n**Environment:**\r\n - OS: macOS \r\n - FastAPI Version 0.10.1:\r\n\r\n```Python\r\nimport fastapi\r\nprint(fastapi.__version__)\r\n```\r\n\r\n- Python version, get it with: 3.7.2\r\n\r\n```bash\r\npython --version\r\n```\r\n\r\n**Additional context**\r\nN/A",
    "labels": [
        "bug",
        "reviewed"
    ],
    "state": "closed",
    "comments": [
        {
            "author": "sebdiem",
            "body": "I can use https://github.com/tiangolo/fastapi/issues/31 as a backup solution but still it seems like a bug to me.",
            "created_at": "2019-04-03T20:13:45Z"
        },
        {
            "author": "tiangolo",
            "body": "I agree with you.\r\n\r\nI just implemented it in PR #168, including tests.\r\n\r\nIt is now available in version `0.17.0` (just released).",
            "created_at": "2019-04-20T18:25:48Z"
        }
    ],
    "comment_count": 2
}
"""